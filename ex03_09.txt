// 작은 원들로 캔버스를 가득 채우기

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  for (let y = 0; y <= height; y += 30) {		// y축으로 반복하여 원 그리기
    for (let x = 0; x <= width; x += 30) {		// x축으로 반복하여 원 그리기
      fill(255, 255, 0);
      ellipse(x, y, 30, 30);
    }
  }
}